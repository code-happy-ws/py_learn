import re
# s='everyone'
# pattern = re.compile('every|eve')
# result = re.search(pattern,s)
p='"a sd" sa "'
# pattern = re.compile('(?:[^"]|.)+')
# result = re.search(pattern, p)
# print(result.group())